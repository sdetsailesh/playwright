{
  "name": "demoqa-playwright-framework",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright automation framework for DemoQA using Page Object Model and custom Extent-style HTML report.",
  "scripts": {
    "test": "npx playwright test",
    "report": "npx playwright show-report",
    "clean": "rm -rf test-results extent-report",
    "build": "npx tsc --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "typescript": "^5.6.0"
  }
}
